{% extends 'base.html.twig' %}

{% block body %}

    <div class="container mt-5">
        <div class="row">
            <div class="col-6">
                <h1>Serie</h1>
                <p>{{ series|length }} </p>
                {% if series is empty %}
                    <p>Il n'y a pas de serie</p>
                {% else %}
                    <p>Il y a des series</p>
                {% endif %}
            </div>
            <div class="col-6">
                <h1>Categorie</h1>
                {% if categories|length > 0  %}
                {% for categorie in categories %}
                    <option value="{{ categorie.id }}">{{ categorie.nom }}</option>
                    <li><a href="{{path('categorie', {id: categorie.id})}}">{{user.nom|capitalize}}</a></li>

                {% endfor %}
                {% else %}
                <p> pas de categories entré</p>
                {% endif %}
               {{ form_start(formCategorie) }}
               {{ form_end(formCategorie) }}


            </div>

        </div>
    </div>
{% endblock %}
